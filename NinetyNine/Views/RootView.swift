//
//  RootView.swift
//  Placeholder shell — replaced once the design system lands.
//

import SwiftUI

struct RootView: View {
    var body: some View {
        Text("99")
            .font(.system(size: 96, weight: .black, design: .serif))
    }
}
